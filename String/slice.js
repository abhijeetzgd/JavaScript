/*
slice() :
*   slice exatract the part of string ,and return the extracted part in new string.
*`  this method takes two argument.
        1.start position(include)
        2.end position(exclude)
*   doesnt change the original array.
*/

let str = 'Apple,Banana,Chole,Bhature,Chicken,Tanduri'
console.log(str.slice(0,7));

var temp = str.slice(0,17)
console.log(temp)