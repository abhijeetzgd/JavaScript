//search() returns the index of first occurance of string
//Finds the first substring match in a regular expression search.
//Search method cannot take second argument which is start position.

const bio  = "i am IT engineering student.IT is booming now adays.stud student take part in IT"

console.log(bio.search('IT'));
console.log(bio.search('stud'));