/*
*if argument is positive numberthen Math.trun() is equivqlent to math.floor()
otherwise Math.trunc() is equivalent to math.ceil()

*it returns integer part if number
*/

console.log(Math.trunc(4.6))
console.log(Math.trunc(6.6))
