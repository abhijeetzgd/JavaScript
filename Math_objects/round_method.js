/*
round() method returns value of of x rounded to its nearest integer
if fraction is greater than or equal to 0.5 then rounded to integer higher than x
*/

let b = 10.34;
let a = 10.5;

console.log(Math.round(a));
console.log(Math.round(b));